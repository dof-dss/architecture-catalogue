---
defaults: &defaults
  memory: 64M
  instances: ((APP_INSTANCES))
  buildpack: php_buildpack
  services:
  - ea-mysql-service
  - ea-es-service

applications:
  - name: ((APP_NAME))
    <<: *defaults
